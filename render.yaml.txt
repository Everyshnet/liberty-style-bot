services:
  - type: worker
    runtime: python
    name: liberty-style-bot
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
